#import "./fragment.graphql"

query assessments($filter: EmployeePerformanceAssessmentFilter, $pagination: Pagination) {
    assessments(filter: $filter, pagination: $pagination) {
        ...EmployeePerformanceAssessment
    }
}
